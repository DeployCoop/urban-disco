#!/usr/bin/env bash
: "${VERBOSITY:=0}"
: "${FETCHR_CACHE:=/tmp}"


main () {
  export THIS_CWD=$(pwd)

  bash -eux repo_list
  helm repo update

  rm -Rf charts
  mkdir charts

  ./custom

  cd ${THIS_CWD}/charts
  bash -eux ../list_fetch
}

time main $@
